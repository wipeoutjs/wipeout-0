Create object
Find template
For each non HTML element in template, create an object, append it to view._templateItems["itemXPath"]
Look for template "TemplateId_html"
	If none exists, create it -
	Place each html element and non element node into it. As for custom objects, replace them with:
	<!-- ko with: _templateItems["itemXPath"] -->
		<!-- ko template: { name: htmlTemplateId } -->
		<!-- /ko -->
	<!-- /ko -->
	Save crated template to script tag TemplateId_html
set htmlTemplateId to TemplateId_html
return ko.bindingHandlers.template...blabla