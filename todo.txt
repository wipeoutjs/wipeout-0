
Debug code
document async templates
event priority
trim template ids
document move
test dom node removed cross browser
todo: unTemplate, are all ko bindings disposed of properly?

workaround for no Mutation events
document custom filters on find
remove freeze
ParentElement

isStartComment() and isEndCOmment() in ko
document user settings

If, poke template

changes:    woInvisible -> shareParentScope
            renderedChildren -> null
            _initialized -> __woBag.initialized
            nodes -> .__woBag.nodes
            getParent/getParents -> changed the way they work with shareParentScope
            
IMINENT ->  add unit tests for bindings
            add move/delete element support (MutationObserver)
            re do MutationObserver polyfill,
            go through TODOs